Modified to take in `pptx` as bytes instead of a path, and to output as a list of tuples (slides) of (markdown text | None, notes | None).

All features may not work as intended, I highly recommend using the original package. (Thank you ssine this is an amazing library)